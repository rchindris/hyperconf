# Configuraiton object definitions used by unit tests.

int_gt3:
  type: int
  validator: |
    value > 3

head:
  name: str
  labels:
    type: str

detector:
  stem: str
  heads:
    type: head
    allow_many: True

train:
  num_epoch: int
  learning_rate: float
